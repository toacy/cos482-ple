version: '2'
services:
  sistemaacademico-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/sistemaAcademico/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sistemaAcademico
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
